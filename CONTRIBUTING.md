# Жобаға үлес қосу (CONTRIBUTING)

**SecureGuard IRP** жобасын дамытуға қызығушылық танытқаныңызға рахмет! Біз ашық бастапқы код қауымдастығының кез келген көмегін құптаймыз. Төменде жұмысты бастауға арналған негізгі нұсқаулықтар келтірілген.

## 🛠 Жоба Құрылымы

Жоба екі негізгі бөліктен тұрады:

1.  **Frontend** (`/` түбірінде): React, TypeScript, Vite.
    -   `components/`: Қайталамалы UI компоненттері (Button, Card, Modal).
    -   `views/`: Негізгі беттер (Dashboard, Incidents, Users).
    -   `context/`: Глобалды күйді басқару (AuthContext).
2.  **Backend** (`/server` папкасында): Node.js, Express.
    -   `index.js`: Сервердің кіру нүктесі және API маршруттары.
    -   `../secureguard.db`: SQLite дерекқор файлы.

## 🚀 Жұмысты бастау

1.  **Репозиторийді Fork жасаңыз**.
2.  **Жергілікті компьютерге клондаңыз**:
    ```bash
    git clone https://github.com/StartUp-kz/secureguard-irp-system.git
    cd secureguard-irp-system
    ```
3.  **Тәуелділіктерді орнатыңыз** (Frontend және Backend үшін ортақ):
    ```bash
    npm install
    ```
4.  **Дерекқорды дайындаңыз** (егер ол бос болса):
    ```bash
    node check_users.js
    ```
    *Бұл скрипт кестелерді құрып, бастапқы админ аккаунтын жасайды.*
5.  **Жобаны іске қосыңыз**:
    ```bash
    npm run start
    ```

## 📏 Кодтау Стандарттары (Style Guide)

*   **TypeScript**: Мүмкіндігінше `any` типін қолданбаңыз. `types.ts` файлында интерфейстерді анықтаңыз.
*   **Компоненттер**: Функционалды компоненттер (React Hooks) қолданыңыз.
*   **Стильдер**: Тек **Tailwind CSS** кластарын қолданыңыз. Арнайы CSS файлдарын жазудан аулақ болыңыз.
*   **Айнымалылар**: `camelCase` (айнымалылар, функциялар) және `PascalCase` (компоненттер, интерфейстер) қолданыңыз.

## 🔄 Pull Request (PR) жіберу

1.  Жаңа функционал үшін жеке branch ашыңыз:
    ```bash
    git checkout -b feature/amazing-feature
    ```
2.  Өзгерістеріңізді енгізіп, commit жасаңыз:
    ```bash
    git commit -m "feat: add amazing new dashboard widget"
    ```
    *(Біз [Conventional Commits](https://www.conventionalcommits.org/) стандартын ұстанамыз: `feat`, `fix`, `docs`, `style`, `refactor`)*
3.  GitHub-қа push жасаңыз және Pull Request ашыңыз.
4.  PR сипаттамасында не істегеніңізді және қандай мәселені шешкеніңізді жазыңыз.

## 🐛 Қателерді хабарлау

Егер қате тапсаңыз, [Issues] бөлімінде келесі ақпаратпен жаңа issue ашыңыз:
*   Қатенің қысқаша сипаттамасы.
*   Қатені қайталау қадамдары (Steps to reproduce).
*   Күтілген нәтиже және нақты нәтиже.
*   Скриншоттар (егер мүмкін болса).

---
Сұрақтарыңыз болса, **Issues** бөлімінде талқылауға қош келдіңіз!
